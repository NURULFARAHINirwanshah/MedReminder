package com.psm.medreminder;

import android.content.Intent;
import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.Button;
import android.widget.EditText;




public class MedicineInfo extends AppCompatActivity {

 /*   private EditText etUserId, etName, etDate, etExpDate, etRn, etDosage, etFrequency, etIndication;

    private Button btnUpdateMed, btnDeleteMed;

    final String appointmentUrl = "http://192.168.43.207/medreminder/medicine_add-view.php?user_id=1";

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.medicine_info);

        Intent intent = getIntent();

        String userID = intent.getStringExtra(userid);
        String medName = intent.getStringExtra(med_name);
        String medDate = intent.getStringExtra(med_date);
        String medExpDate = intent.getStringExtra(med_expdate);
        String medRn = intent.getStringExtra(med_rn);
        String medDosage = intent.getStringExtra(med_dosage);
        String medFrequency = intent.getStringExtra(med_frequency);
        String medIndication = intent.getStringExtra(med_indication);


        etUserId = (EditText)findViewById(R.id.etUserId);
        etName = (EditText)findViewById(R.id.etName);
        etDate = (EditText)findViewById(R.id.etDate);
        etExpDate = (EditText)findViewById(R.id.etExpDate);
        etRn = (EditText)findViewById(R.id.etRn);
        etDosage = (EditText)findViewById(R.id.etDosage);
        etFrequency = (EditText)findViewById(R.id.etFrequency);
        etIndication = (EditText)findViewById(R.id.etIndication); */



    /*    button_update = (Button) findViewById(R.id.button_updateschedule);
        button_update.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                updateSchedule();
            }
        });

        button_delete = (Button) findViewById(R.id.button_deleteschedule);
        button_delete.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                deleteSchedule();
            }
        }); */


 /*       etUserId.setText(userID);
        etName.setText(medName);
        etDate.setText(medDate);
        etExpDate.setText(medExpDate);
        etRn.setText(medRn);
        etDosage.setText(medDosage);
        etFrequency.setText(medFrequency);
        etIndication.setText(medIndication);

    } */
}